/**
 * Blog article: kubernetes-basics-dotnet-developers
 * Auto-generated by scripts/split-blog-articles.js. Do not edit the content here by hand if you run the split script again.
 */

export default {
  slug: "kubernetes-basics-dotnet-developers",
  title: "Kubernetes Basics for .NET Developers",
  excerpt: "Pods, Deployments, Services, and ConfigMaps. Running .NET apps on AKS and local Kubernetes (minikube, kind).",
  date: "2025-04-03",
  topic: "Cloud",
  keywords: ["Kubernetes Basics for .NET Developers", "Kubernetes Basics Dotnet Developers", "Kubernetes Basics Dotnet Developers best practices", "how to kubernetes basics dotnet developers", "kubernetes basics dotnet developers in .NET", "kubernetes basics dotnet developers guide", "kubernetes basics dotnet developers for enterprise", "kubernetes basics dotnet developers patterns", "when to use kubernetes basics dotnet developers", "kubernetes basics dotnet developers tutorial", "kubernetes basics dotnet developers examples", "kubernetes basics dotnet developers in C#", "kubernetes basics dotnet developers overview", "kubernetes basics dotnet developers implementation", "understanding kubernetes basics dotnet developers", "kubernetes basics dotnet developers for developers", "kubernetes basics dotnet developers checklist", "kubernetes basics dotnet developers tips", "kubernetes basics dotnet developers deep dive", "kubernetes basics dotnet developers comparison", "kubernetes basics dotnet developers vs alternatives", "kubernetes basics dotnet developers .NET Core", "kubernetes basics dotnet developers Azure", "kubernetes basics dotnet developers explained", "kubernetes basics dotnet developers when to use", "kubernetes basics dotnet developers enterprise", "kubernetes basics dotnet developers .NET", "what is kubernetes basics dotnet developers", "kubernetes basics dotnet developers summary", "kubernetes basics dotnet developers introduction", "kubernetes basics dotnet developers fundamentals", "kubernetes basics dotnet developers step by step", "kubernetes basics dotnet developers complete guide", "kubernetes basics dotnet developers for beginners", "kubernetes basics dotnet developers advanced", "kubernetes basics dotnet developers production", "kubernetes basics dotnet developers real world", "kubernetes basics dotnet developers example code", "kubernetes basics dotnet developers C# example", "kubernetes basics dotnet developers .NET example", "learn kubernetes basics dotnet developers", "kubernetes basics dotnet developers learn", "kubernetes basics dotnet developers reference", "kubernetes basics dotnet developers cheat sheet", "kubernetes basics dotnet developers pitfalls", "kubernetes basics dotnet developers common mistakes", "kubernetes basics dotnet developers performance", "kubernetes basics dotnet developers optimization", "kubernetes basics dotnet developers security", "kubernetes basics dotnet developers testing", "kubernetes basics dotnet developers unit test", "kubernetes basics dotnet developers integration", "kubernetes basics dotnet developers migration", "kubernetes basics dotnet developers from scratch", "kubernetes basics dotnet developers 2024", "kubernetes basics dotnet developers 2025", "best kubernetes basics dotnet developers", "kubernetes basics dotnet developers best", "pro kubernetes basics dotnet developers", "kubernetes basics dotnet developers expert", "kubernetes basics dotnet developers consultant", "kubernetes basics dotnet developers services", "kubernetes basics dotnet developers course", "kubernetes basics dotnet developers workshop", "kubernetes basics dotnet developers webinar", "kubernetes basics dotnet developers blog", "kubernetes basics dotnet developers article", "kubernetes basics dotnet developers post", "why kubernetes basics dotnet developers", "when kubernetes basics dotnet developers", "where kubernetes basics dotnet developers", "kubernetes basics dotnet developers in .NET 6", "kubernetes basics dotnet developers in .NET 7", "kubernetes basics dotnet developers in .NET 8", "kubernetes basics dotnet developers for C#", "kubernetes basics dotnet developers for Angular", "kubernetes basics dotnet developers for Vue", "kubernetes basics dotnet developers for React", "kubernetes basics dotnet developers for Azure", "kubernetes basics dotnet developers for microservices", "kubernetes basics dotnet developers for API", "kubernetes basics dotnet developers for database", "kubernetes basics dotnet developers for testing", "kubernetes basics dotnet developers for DevOps", "kubernetes basics dotnet developers for senior developers", "kubernetes basics dotnet developers for team", "kubernetes basics dotnet developers for production", "kubernetes basics dotnet developers for scale", "kubernetes basics dotnet developers for refactoring", "kubernetes basics dotnet developers for enterprise applications", "kubernetes basics dotnet developers for startup", "kubernetes basics dotnet developers in 2024", "kubernetes basics dotnet developers in 2025", "kubernetes basics dotnet developers in 2026", "kubernetes basics dotnet developers code sample", "kubernetes basics dotnet developers code example", "kubernetes basics dotnet developers sample code", "kubernetes basics dotnet developers full example", "kubernetes basics dotnet developers working example", "kubernetes basics dotnet developers practical kubernetes basics dotnet developers", "kubernetes basics dotnet developers real world example", "kubernetes basics dotnet developers use case", "kubernetes basics dotnet developers use cases", "kubernetes basics dotnet developers scenario", "kubernetes basics dotnet developers scenarios", "kubernetes basics dotnet developers pattern", "kubernetes basics dotnet developers approach", "kubernetes basics dotnet developers approaches", "kubernetes basics dotnet developers strategy", "kubernetes basics dotnet developers strategies", "kubernetes basics dotnet developers technique", "kubernetes basics dotnet developers techniques", "kubernetes basics dotnet developers method", "kubernetes basics dotnet developers methods", "kubernetes basics dotnet developers solution", "kubernetes basics dotnet developers solutions", "kubernetes basics dotnet developers implementation guide", "kubernetes basics dotnet developers getting started", "kubernetes basics dotnet developers quick start", "kubernetes basics dotnet developers overview guide", "kubernetes basics dotnet developers comprehensive guide", "kubernetes basics dotnet developers detailed guide", "kubernetes basics dotnet developers practical guide", "kubernetes basics dotnet developers developer guide", "kubernetes basics dotnet developers engineer guide", "kubernetes basics dotnet developers architect guide", "kubernetes basics dotnet developers for architects", "kubernetes basics dotnet developers for backend", "kubernetes basics dotnet developers for tech leads", "kubernetes basics dotnet developers for senior devs", "benefits of kubernetes basics dotnet developers", "advantages of kubernetes basics dotnet developers", "alternatives to kubernetes basics dotnet developers", "compared to kubernetes basics dotnet developers", "intro to kubernetes basics dotnet developers", "basics of kubernetes basics dotnet developers", "kubernetes basics dotnet developers tips and tricks", "kubernetes basics dotnet developers production-ready", "kubernetes basics dotnet developers enterprise-grade", "kubernetes basics dotnet developers with Docker", "kubernetes basics dotnet developers with Kubernetes", "kubernetes basics dotnet developers in ASP.NET Core", "kubernetes basics dotnet developers with Entity Framework", "kubernetes basics dotnet developers with EF Core", "kubernetes basics dotnet developers modern", "kubernetes basics dotnet developers updated", "kubernetes basics dotnet developers latest", "kubernetes basics dotnet developers walkthrough", "kubernetes basics dotnet developers hands-on", "kubernetes basics dotnet developers practical examples", "kubernetes basics dotnet developers real-world examples", "kubernetes basics dotnet developers common pitfalls", "kubernetes basics dotnet developers gotchas", "kubernetes basics dotnet developers FAQ", "kubernetes basics dotnet developers FAQs", "kubernetes basics dotnet developers Q&A", "kubernetes basics dotnet developers interview questions", "kubernetes basics dotnet developers interview", "kubernetes basics dotnet developers certification", "kubernetes basics dotnet developers training", "kubernetes basics dotnet developers video", "kubernetes basics dotnet developers series", "kubernetes basics dotnet developers part 1", "kubernetes basics dotnet developers core concepts", "kubernetes basics dotnet developers key concepts", "kubernetes basics dotnet developers recap", "kubernetes basics dotnet developers takeaways", "kubernetes basics dotnet developers conclusion", "kubernetes basics dotnet developers next steps", "kubernetes basics dotnet developers further reading", "kubernetes basics dotnet developers resources", "kubernetes basics dotnet developers tools", "kubernetes basics dotnet developers libraries", "kubernetes basics dotnet developers frameworks", "kubernetes basics dotnet developers NuGet", "kubernetes basics dotnet developers package", "kubernetes basics dotnet developers GitHub", "kubernetes basics dotnet developers open source", "kubernetes basics dotnet developers community", "kubernetes basics dotnet developers Microsoft docs", "kubernetes basics dotnet developers documentation", "kubernetes basics dotnet developers official guide", "kubernetes basics dotnet developers official tutorial", "kubernetes basics developers with .NET Core", "kubernetes basics dotnet developers ASP.NET Core", "Kubernetes", "Kubernetes guide", "Kubernetes tutorial", "Kubernetes best practices", "Kubernetes in .NET", "Kubernetes in C#", "Kubernetes for developers", "Kubernetes examples", "Kubernetes patterns", "Kubernetes overview", "Kubernetes introduction", "Kubernetes deep dive", "Kubernetes explained", "Kubernetes how to", "Kubernetes what is"],
  relatedServices: ["azure-cloud-architecture","microservices-architecture"],
  relatedProjects: ["bat-inhouse-app"],
  relatedArticleSlugs: ["azure-microservices-best-practices","ci-cd-azure-devops"],
  author: "Waqas Ahmad",
  content: `## Introduction

This guidance is relevant when the topic of this article applies to your system or design choices; it breaks down when constraints or context differ. I've applied it in real projects and refined the takeaways over time (as of 2026).

**Kubernetes** (often abbreviated **K8s**) is the dominant container orchestration platform. If you build .NET applications and want to run them in production at scale, Kubernetes is likely part of your infrastructure—whether on Azure Kubernetes Service (AKS), AWS EKS, Google GKE, or on-premises.

This article is for .NET developers who are new to Kubernetes. We cover the core concepts: what Kubernetes does, how Pods, Deployments, and Services work, how to configure apps with ConfigMaps and Secrets, and how to run .NET containers on Kubernetes.

## Topics covered

- [Decision Context](#decision-context)
- [What is Kubernetes and why use it?](#what-is-kubernetes-and-why-use-it)
- [Core concepts: Pods, Deployments, Services](#pods-deployments-and-services)
- [ConfigMaps and Secrets](#configmaps-and-secrets)
- [Running .NET on Kubernetes](#running-net-on-kubernetes)
- [Health checks and resource limits](#health-checks-and-resource-limits)
- [Running Kubernetes locally](#running-kubernetes-locally)
- [Deploying to AKS](#deploying-to-aks)
- [Enterprise best practices](#enterprise-best-practices)
- [Common issues and troubleshooting](#common-issues-and-challenges)
- [Frequently Asked Questions](#frequently-asked-questions)

## Decision Context

- **System scale:** Containerised apps (e.g. .NET) from a few pods to many; single cluster or multi-environment. Applies when you’re moving from “run on a VM” to orchestrated containers.
- **Team size:** Dev and ops (or platform); someone must own manifests, secrets, and cluster health. Works when the team can use kubectl and maintain YAML (or Helm).
- **Time / budget pressure:** Fits when you need scaling, self-healing, or portability; breaks down when the team has no container or Kubernetes experience—then start with managed Kubernetes (e.g. AKS) and simple deployments.
- **Technical constraints:** .NET in Docker; Kubernetes (or AKS, EKS, GKE); ConfigMaps, Secrets, health checks. Assumes you can build container images and deploy to a cluster.
- **Non-goals:** This article does not optimise for non-container workloads or for deep cluster operations; it focuses on .NET developers getting apps onto Kubernetes.

## What is Kubernetes and why use it?

**Kubernetes** is a container orchestrator. It takes your containerised applications (Docker images) and runs them across a cluster of machines, handling:

| Capability | What it does |
|------------|--------------|
| **Scheduling** | Decides which node runs which container |
| **Scaling** | Runs multiple replicas; scales up/down based on load |
| **Self-healing** | Restarts crashed containers; replaces unhealthy nodes |
| **Load balancing** | Distributes traffic across replicas |
| **Rolling updates** | Deploys new versions without downtime |
| **Service discovery** | Containers find each other by name, not IP |
| **Config management** | Injects config and secrets into containers |

**Why .NET developers should care:** Your .NET API runs in a Docker container. Kubernetes runs that container, ensures it stays running, scales it when traffic increases, and routes requests to it.

## Pods, Deployments, and Services

| Resource | Purpose | Analogy |
|----------|---------|---------|
| **Pod** | Runs one or more containers | A single instance of your app |
| **Deployment** | Manages a set of Pods | The "desired state" manager |
| **Service** | Exposes Pods to the network | A stable address for your app |

**Pod**: Smallest deployable unit; one or more containers sharing network and storage. For .NET apps, typically one container per Pod.

**Deployment**: Manages replicas of Pods; handles rolling updates and rollback. You define the desired state (3 replicas); Kubernetes maintains it.

**Service**: Stable network identity for Pods (ClusterIP, LoadBalancer, NodePort). Pods are ephemeral; Services provide a stable address.

\`\`\`yaml
# Deployment example
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-api
  template:
    metadata:
      labels:
        app: my-api
    spec:
      containers:
      - name: api
        image: myregistry.azurecr.io/my-api:latest
        ports:
        - containerPort: 8080
\`\`\`

## ConfigMaps and Secrets

**ConfigMaps** store non-sensitive configuration (app settings, feature flags). **Secrets** store sensitive data (connection strings, API keys). Both can be mounted as environment variables or files.

\`\`\`yaml
# configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: my-api-config
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Logging__LogLevel__Default: "Information"
\`\`\`

\`\`\`yaml
# secret.yaml (values are base64-encoded)
apiVersion: v1
kind: Secret
metadata:
  name: my-api-secrets
type: Opaque
data:
  ConnectionStrings__Default: U2VydmVyPW15c2VydmVyO0RhdGFiYXNlPW15ZGI=
\`\`\`

### Using ConfigMap and Secret in a Deployment

\`\`\`yaml
spec:
  containers:
  - name: api
    image: myregistry.azurecr.io/my-api:v1.0.0
    envFrom:
    - configMapRef:
        name: my-api-config
    - secretRef:
        name: my-api-secrets
\`\`\`

**Important:** Base64 is not encryption. In production, use **Azure Key Vault** with the **CSI Secrets Store Driver** or **Managed Identity**.

## Running .NET on Kubernetes

To run a .NET app on Kubernetes:

1. **Containerise** your app (Dockerfile)
2. **Build and push** to a registry (ACR, Docker Hub)
3. **Create Kubernetes manifests** (Deployment, Service)
4. **Apply** with \`kubectl apply -f\`

### Dockerfile for ASP.NET Core

\`\`\`dockerfile
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY ["MyApi/MyApi.csproj", "MyApi/"]
RUN dotnet restore "MyApi/MyApi.csproj"
COPY . .
RUN dotnet publish -c Release -o /app/publish

FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS runtime
WORKDIR /app
COPY --from=build /app/publish .
EXPOSE 8080
ENV ASPNETCORE_URLS=http://+:8080
ENTRYPOINT ["dotnet", "MyApi.dll"]
\`\`\`

### Build and push

\`\`\`bash
docker build -t myregistry.azurecr.io/my-api:v1.0.0 .
az acr login --name myregistry
docker push myregistry.azurecr.io/my-api:v1.0.0
\`\`\`

## Health checks and resource limits

### Health probes

| Probe | Purpose | If it fails |
|-------|---------|-------------|
| **Liveness** | Is the container alive? | Kubernetes restarts it |
| **Readiness** | Is it ready for traffic? | Kubernetes stops sending traffic |
| **Startup** | Has it started? | Delays liveness/readiness |

\`\`\`yaml
livenessProbe:
  httpGet:
    path: /health/live
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health/ready
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 5
\`\`\`

### Resource requests and limits

\`\`\`yaml
resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "1000m"
\`\`\`

## Running Kubernetes locally

| Tool | Description |
|------|-------------|
| **Docker Desktop** | Built-in Kubernetes (enable in settings) |
| **minikube** | Runs a single-node cluster in a VM |
| **kind** | Kubernetes IN Docker (fast startup) |
| **k3d** | Lightweight k3s in Docker |

## Deploying to AKS

\`\`\`bash
# Create AKS cluster
az aks create --resource-group my-rg --name my-aks --node-count 3

# Get credentials
az aks get-credentials --resource-group my-rg --name my-aks

# Attach ACR
az aks update --resource-group my-rg --name my-aks --attach-acr myregistry

# Deploy
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
\`\`\`

## Enterprise best practices

**1. Use namespaces** to isolate workloads (dev, staging, prod).

**2. Always set resource requests and limits** so the scheduler can make good decisions.

**3. Use health probes** (liveness, readiness, startup) so Kubernetes can detect unhealthy Pods.

**4. Store secrets in Azure Key Vault**, not Kubernetes Secrets (base64 is not encryption).

**5. Use Managed Identity** for Azure access instead of storing credentials.

**6. Enable autoscaling** (HPA for Pods, Cluster Autoscaler for nodes).

**7. Use Ingress** for HTTP routing instead of multiple LoadBalancers.

**8. Monitor with Prometheus/Grafana** or Azure Monitor.

## Common issues and troubleshooting

| Issue | Symptoms | Fix |
|-------|----------|-----|
| **ImagePullBackOff** | Pod stuck pulling image | Check image name/tag; verify registry auth |
| **CrashLoopBackOff** | Pod keeps restarting | Check logs (\`kubectl logs\`); app is crashing |
| **Pending Pod** | Pod stuck in Pending | Not enough resources; check node capacity |
| **Readiness failing** | Pod not receiving traffic | Check health endpoint; verify dependencies |
| **OOMKilled** | Container killed | Increase memory limit; check for leaks |

### Debugging commands

\`\`\`bash
kubectl get pods
kubectl describe pod <pod-name>
kubectl logs <pod-name>
kubectl exec -it <pod-name> -- /bin/sh
kubectl get events --sort-by=.metadata.creationTimestamp
\`\`\`

## Summary

Kubernetes runs containerised apps with scheduling, scaling, and self-healing. .NET apps run as Pods (one or more containers); Deployments manage replicas and rolling updates; Services expose them with a stable network name. Use ConfigMaps and Secrets for config; set health checks and resource limits so the cluster can manage the app. For .NET developers, the path is: containerise the app, write a Deployment and Service, deploy to AKS (or another cluster), and use kubectl to inspect and debug.

## Position & Rationale

I use **Kubernetes** (or AKS) when we need orchestration: multiple replicas, rolling updates, self-healing, and a single place to define config and secrets. I prefer **managed Kubernetes** (AKS, EKS, GKE) over self-managed so we don’t run the control plane. For .NET I run one process per container and one main container per Pod unless we have a clear sidecar need. I set **resource requests and limits** and **liveness/readiness** probes so the scheduler and load balancer can make good decisions. I avoid running stateful or persistent data in Pods without a proper storage abstraction (PVC, StatefulSet) and I don’t put secrets in environment variables in plain text—use Kubernetes Secrets or a vault integration.

## Trade-Offs & Failure Modes

**Kubernetes** adds YAML and operational concepts (Pods, Deployments, Services); you gain portability and orchestration. **Managed Kubernetes** reduces control-plane burden but ties you to a cloud provider. Failure modes: Pods without resource limits (one hungry pod can starve others); missing or wrong readiness probes (traffic sent to pods that aren’t ready); secrets in env as plain text; ImagePullBackOff or CrashLoopBackOff without a runbook.

## What Most Guides Miss

Most guides show Pod/Deployment/Service but don’t stress **readiness vs liveness**—readiness controls when the Pod gets traffic; liveness controls when it gets restarted. Getting them wrong leads to restarts or traffic to broken pods. Another gap: **resource requests and limits**—if you don’t set them, the scheduler can overcommit and nodes run out of memory. **Debugging** (kubectl logs, describe, events) is often mentioned but not as a first resort when “it doesn’t work.”

## Decision Framework

- **If you’re deploying a .NET app** → Containerise it; create a Deployment (replicas, image, env) and a Service (ClusterIP or LoadBalancer).
- **For config** → ConfigMaps for non-sensitive; Secrets for sensitive (or integrate with Key Vault).
- **For health** → Set liveness and readiness probes so Kubernetes can restart and route traffic correctly.
- **For resources** → Set requests and limits (CPU, memory) so the scheduler and node don’t overcommit.
- **When things fail** → Use kubectl get/describe/logs/events to see why Pods are Pending, CrashLoopBackOff, or not receiving traffic.

## Key Takeaways

- **Pods** run containers; **Deployments** manage replica count and rolling updates; **Services** give a stable network name.
- Use **ConfigMaps** and **Secrets** for config; set **liveness** and **readiness** and **resource limits**.
- For .NET: one container per Pod unless you have a sidecar; use AKS (or similar) for managed Kubernetes.
- Debug with **kubectl get pods**, **describe pod**, **logs**, and **events** when Pods don’t start or receive traffic.

## When I Would Use This Again — and When I Wouldn't

I’d use **Kubernetes** (AKS) again for any production .NET workload that needs scaling, rolling updates, and a standard place to run containers. I’d use the same pattern: Deployment + Service, ConfigMaps/Secrets, health checks, and resource limits. I wouldn’t run stateful data in Pods without a proper storage and backup story. I also wouldn’t adopt Kubernetes for a single small app with no scaling or portability need—simpler hosting (App Service, single VM) can be enough.

## Frequently Asked Questions

### What is a Pod?

A Pod is the smallest deployable unit in Kubernetes; it runs one or more containers that share network and storage. Usually one container per Pod for .NET apps.

### What is a Deployment?

A Deployment manages a set of Pod replicas: desired count, rolling updates, rollback. You define a template (container image, env, ports); Kubernetes creates Pods to match.

### What is a Kubernetes Service?

A Service gives Pods a stable network name and IP; other Pods or external clients reach them via the Service. Types: ClusterIP (internal), LoadBalancer (external), NodePort.

### What is the difference between ClusterIP and LoadBalancer?

\`ClusterIP\` is internal-only (accessible within the cluster). \`LoadBalancer\` creates a cloud load balancer with an external IP. Use ClusterIP for internal APIs; LoadBalancer for external APIs.

### How do I configure my .NET app in Kubernetes?

Use **ConfigMaps** for non-sensitive config and **Secrets** for sensitive data. Reference them with \`envFrom\` or mount as files. For production, use **Azure Key Vault** with the CSI driver.

### What are liveness and readiness probes?

**Liveness probe**: Is the container alive? If it fails, Kubernetes restarts the container. **Readiness probe**: Is the container ready for traffic? If it fails, Kubernetes stops sending traffic.

### How do I scale my app in Kubernetes?

Manually: \`kubectl scale deployment my-api --replicas=5\`. Automatically: Use **Horizontal Pod Autoscaler (HPA)** to scale based on CPU.

### What is AKS?

**Azure Kubernetes Service (AKS)** is Azure's managed Kubernetes. Azure manages the control plane; you manage worker nodes.

### Why is my Pod stuck in ImagePullBackOff?

The image cannot be pulled. Check image name/tag; verify registry auth with \`imagePullSecrets\` or ACR integration.

### Why is my Pod in CrashLoopBackOff?

The container is crashing on startup. Check logs with \`kubectl logs <pod-name>\`.

### What is the difference between requests and limits?

**Requests** are guaranteed resources. **Limits** are maximum resources. If memory limit is exceeded, the container is OOMKilled.

### How do I run Kubernetes locally?

Use **Docker Desktop**, **minikube**, **kind**, or **k3d**. Docker Desktop is easiest for Windows/Mac.

### What is an Ingress?

An **Ingress** routes HTTP traffic to Services based on host/path. Use one Ingress controller instead of multiple LoadBalancers.`,
  faqs: [
  {
    "question": "What is a Pod?",
    "answer": "A Pod is the smallest deployable unit in Kubernetes; it runs one or more containers that share network and storage."
  },
  {
    "question": "What is a Deployment?",
    "answer": "A Deployment manages a set of Pod replicas: desired count, rolling updates, rollback."
  },
  {
    "question": "What is a Kubernetes Service?",
    "answer": "A Service gives Pods a stable network name and IP. Types: ClusterIP (internal), LoadBalancer (external), NodePort."
  },
  {
    "question": "What is the difference between ClusterIP and LoadBalancer?",
    "answer": "ClusterIP is internal-only. LoadBalancer creates a cloud load balancer with an external IP."
  },
  {
    "question": "How do I configure my .NET app in Kubernetes?",
    "answer": "Use ConfigMaps for non-sensitive config and Secrets for sensitive data. For production, use Azure Key Vault."
  },
  {
    "question": "What are liveness and readiness probes?",
    "answer": "Liveness: Is the container alive? Readiness: Is it ready for traffic? Kubernetes uses these to restart unhealthy containers."
  },
  {
    "question": "How do I scale my app?",
    "answer": "Manually: kubectl scale deployment my-api --replicas=5. Automatically: Use HPA."
  },
  {
    "question": "What is AKS?",
    "answer": "Azure Kubernetes Service is Azure managed Kubernetes. Azure manages the control plane."
  },
  {
    "question": "Why is my Pod in ImagePullBackOff?",
    "answer": "Image cannot be pulled. Check image name/tag and registry auth."
  },
  {
    "question": "Why is my Pod in CrashLoopBackOff?",
    "answer": "Container is crashing. Check logs with kubectl logs."
  },
  {
    "question": "What is the difference between requests and limits?",
    "answer": "Requests are guaranteed. Limits are maximum. Exceeding memory limit causes OOMKilled."
  },
  {
    "question": "How do I run Kubernetes locally?",
    "answer": "Use Docker Desktop, minikube, kind, or k3d."
  },
  {
    "question": "What is an Ingress?",
    "answer": "Routes HTTP traffic to Services based on host/path."
  }
]
}
